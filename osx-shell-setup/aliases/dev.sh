alias p='pnpm'
alias i='pnpm install'
alias d='pnpm dev'
alias b='pnpm build'
alias build='pnpm build'
alias start='pnpm start'
alias rmall='rm -rf node_modules ; rm -rf .next;'
alias restart='rmall ; pnpm install ; pnpm dev'
alias rebuild='rmall ; pnpm install ; pnpm build ; pnpm dev'
